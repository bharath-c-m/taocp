package art.of.programming.ds;

public class DoubleNode {
	DoubleNode prev;
	DoubleNode next;
	int val;

	/*public String toString() {
		if(prev == null)
			return "BOF-"+val+next.toString();
		else if(next == null)
			return "-"+val+"-EOF";
		return "-"+val+next.toString();
	}*/
	
}
